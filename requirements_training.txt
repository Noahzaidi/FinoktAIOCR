# Additional requirements for PyTorch-based OCR model training
# Install with: pip install -r requirements_training.txt

# Core requirements (from base requirements.txt should already be installed)
# fastapi>=0.104.1
# uvicorn[standard]>=0.24.0
# python-doctr[torch]>=0.7.0
# python-multipart>=0.0.6
# jinja2>=3.1.2
# Pillow>=10.1.0

# PyTorch (choose ONE option below based on your system)

# Option 1: CUDA 11.8 (NVIDIA GPU - Recommended for fast training)
torch>=2.1.0
torchvision>=0.16.0
--extra-index-url https://download.pytorch.org/whl/cu118

# Option 2: CPU only (uncomment if you don't have NVIDIA GPU)
# torch>=2.1.0+cpu
# torchvision>=0.16.0+cpu
# --extra-index-url https://download.pytorch.org/whl/cpu

# Additional dependencies
numpy>=1.24.0
tqdm>=4.66.0  # Progress bars during training

# Optional but recommended
tensorboard>=2.15.0  # For training visualization
matplotlib>=3.8.0  # For plotting training curves

